<% @books.each do |book| %>
  <section class="books-index" id="book-<%= book.id %>">
    <h3><%= book.title %></h3>
      <% book.authors.each do |author| %>
        <h5><%= author.name %></h5>
      <% end %>
      <p>Pages: <%= book.page_count %></p>
      <p>Year: <%= book.year %></p>
  </section>
<% end %>
